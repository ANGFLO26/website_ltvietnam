# FINAL ISSUE REGISTER — CLAUDE INDEPENDENT VERIFICATION OF v0.4.1

**Verifier:** Claude (independent, Round 7) · **Date:** 2026-07-25 · **Candidate:** `planning/implementation/v0.4.1/`

Scope of this register: **new findings only**. Issues already closed and independently re-verified in this round — FV-01 through FV-14 and RI-01 through RI-03 — are not repeated here. Their verification is recorded in `CLAUDE_FINAL_VERIFICATION.md` §§D–G.

**Totals:** Critical 0 · High 0 · Medium 4 · Low 2

`Blocks v1.0` column semantics:
- **CONTENT** — must be present in the assembled v1.0 baseline; does **not** block the promotion decision.
- **TEXT** — literal correction to apply while assembling; does not block promotion.
- No finding in this register blocks the user's promotion decision.

---

| ID | Severity | File/Section | Issue | Evidence | Impact | Required action | Blocks v1.0 |
|---|---|---|---|---|---|---|---|
| **CF-01** | **MEDIUM** | `00` §1 (precedence); `01` §§C.4/A20; `03` §2; `12` §§2–3 | No Approved-document divergence register. Three deliberate v0.4.1 refinements contradict Approved `doc/06`, and none is declared. Because the plan's own precedence ranks Backend/API **above** the plan and above D1–D20, a literal application resolves against the plan and reinstates the very defects RI-01 and FV-02 closed. | Precedence chain `00:14`. (a) `doc/06:202-203` orders `Validate → CAPTCHA → Rate limit → Kiểm idempotency_key`; plan `01:122-131` reverses it. (b) `doc/06:119` defines `/health/ready` as "readiness: DB/storage/outbox/email"; plan `03:24`, `12:31` narrows it to config+PostgreSQL and states it "never checks storage/SMTP/worker". (c) `/health/ready/media` and `/health/worker` (`03:25-26`, `12:32-33`) do not exist in the Approved API surface. Mitigating: ADR-003 `doc/09:184` requires only CAPTCHA+rate-limit before the **write transaction**, which the plan preserves for new keys (`01:129-131`) — so no ADR-level conflict exists. | An implementer resolving a conflict by the stated precedence would implement `doc/06`'s ordering, reintroducing the response-lost-retry lead loss (RI-01), and would couple `/health/ready` to storage/SMTP, reintroducing core-API eviction on non-core failure (FV-02/R-33). Both are the exact HIGH failures this correction round closed. | Add an "Approved-document reconciliation register" section to `01`, listing each divergence with its Approved section reference, engineering rationale, and an Approved-document governance backlog gate — modelled on the existing FV-13/R-22 treatment (`10:71-74`, `09:30`). Add one precedence sentence so a declared divergence is not overridden by Backend/API. List `/media/*` in the register as additive-with-no-Approved-conflict (`doc/05:71-73` stores `public_url`/`storage_path` but prescribes no public prefix). | **CONTENT** |
| **CF-02** | **MEDIUM** | `01` §C.1 (FC-01) | `Idempotency-Key` format, entropy and validation are unspecified. The plan states only that the client creates the key once and reuses it on retry. It also never mentions the second transport the Approved API allows. | `01:71` is the complete statement. Approved sources already settle most of it and the plan restates neither: `doc/06:192` — `Header: Idempotency-Key: <uuid>  (hoặc body.request_id)`; `doc/05:775` — `idempotency_key VARCHAR(100) NOT NULL UNIQUE` (`VARCHAR`, not `CITEXT` as used for `email` at `doc/05:764`, so comparison is byte-exact/case-sensitive; max length 100; empty string not excluded by `NOT NULL`). No mandatory test in `06` exercises key validation. | A conforming implementation may accept a low-entropy key such as `"1"`. A second, unrelated submitter reusing that key with a different payload receives `409 IDEMPOTENCY_KEY_REUSED` — a lost lead, precisely the failure ADR-003 and D19 exist to prevent. Also permits an empty-string key and leaves header-vs-body precedence undefined. | Insert the key-contract paragraph into `01` §C.1: UUID v4 canonical lowercase hyphenated (36 chars, ≥122 bits randomness) per Approved `06`; hard maximum 100 chars per the column; reject absent/empty/whitespace-only/over-length/non-conforming with a stable `400` **before** the durable lookup; never generate or substitute a key; byte-exact case-sensitive comparison, never trimmed or lowercased; one key per logical submission for its whole retry lifetime; accept `Idempotency-Key` header or `body.request_id`, header authoritative when both present, mismatch rejected as `400`. Add one unit test (malformed/empty/over-length/case-variant rejected before lookup) and one API test (both transports, precedence, mismatch). Exact text in `V1_0_PROMOTION_RECOMMENDATION.md` Delta 2. | **CONTENT** |
| **CF-03** | **MEDIUM** | `01` §C.4 Step 2 (FC-02) | "Original stable result" is self-referential and never defined. `01:127` constrains the replay to the "stable result **đã định nghĩa**", but no definition exists in any of the 19 files. | `01:125`, `01:127`, `01:143`. Searched all 19 files — the only concrete signal is `06:47` ("D19 **202** replay"), which pins the status and nothing else. Approved `doc/06:208` already defines it: `202 Accepted { request_id, message }`, with the replay semantic at `doc/06:203`. | The constraint "không trả dữ liệu ngoài stable result" (`01:143`) is unenforceable as written — no reviewer can fail an implementation against an undefined target. Two concrete risks: (i) an implementer may persist a raw response body to guarantee stability, duplicating PII and colliding with R-18 (`09:26`); (ii) a replay whose body varies with `outbox.status` or `inquiries.email_status` is not stable, breaking the retry contract. | Insert the stable-result definition into `01` §C.4 Step 2: HTTP `202`, A24 envelope, `{ request_id, message }` per Approved `06`, `request_id` echoing the client key verbatim, `message` a static locale-appropriate acknowledgement; no Inquiry UUID and no personal data of any kind; byte-identical to the original and invariant to outbox/email/worker state and elapsed time; reconstructed deterministically from the key plus the committed Inquiry row so **no raw response body is stored**; replay recorded in internal logs/metrics only, with no public replay marker (a marker would be a key-existence oracle). Exact text in `V1_0_PROMOTION_RECOMMENDATION.md` Delta 3. | **CONTENT** |
| **CF-04** | **MEDIUM** | `01` §C.7; `04` P7 (FC-03) | Attempt state/outcome model is ambiguous in three ways, and the result-transaction scope is stated three different ways without ever being pinned. | (1) Two names, one concept: `01:173` writes `state='started'`, but the authoritative field list at `04:150` contains `outcome` and no `state`. (2) `unknown` is overloaded — a system state at `01:182`/`04:152`, a manual outcome at `04:156`. (3) One flat 7-value list at `01:185`/`04:153` mixes machine-observed states with human decisions, losing the actor distinction `01:185` itself demands. (4) Transaction scope: `01:180` "rồi cập nhật outbox/inquiry status theo atomic rule phù hợp"; `04:152` "and then coordinates"; `17:23` "use a subsequent transaction" — none states that attempt result, `inquiry_outbox.status` and `inquiries.email_status` commit in one transaction. | Day-one implementation ambiguity: the column name is genuinely undetermined, and "provider accepted, later confirmed duplicate by an operator" cannot be expressed without destroying the first fact. If the three status writes are split across transactions, an attempt can read `accepted` while the outbox stays `processing`. Contained, not unsafe: that split state already falls inside the reconciliation envelope (`06:135`, `01:190`, no-blind-resend), so it produces operational noise and inconsistent reporting rather than lost or duplicated email. | Replace the flat list with three orthogonal fields: `attempt_state` (`started` → `accepted`\|`failed`\|`unknown` → `resolved`), `provider_outcome` (`accepted`, `rejected`, `timeout`, `error`, null until the call returns), `manual_resolution` (`confirmed-sent`, `confirmed-duplicate`, `confirmed-not-sent`, `unknown`; operator-written only, always with actor/time/reason; sets `attempt_state='resolved'` and never rewrites `provider_outcome`). Report system-`unknown` and manual-`unknown` separately. Align `04:150` to the same vocabulary. Add the explicit sentence that attempt result + outbox status + inquiry email status are written in **one single result transaction**, distinct from the attempt-start transaction and opened only after the provider call returns. Add one DB test for atomic three-way status commit and one asserting manual resolution preserves `provider_outcome`. Exact text in `V1_0_PROMOTION_RECOMMENDATION.md` Delta 4. | **CONTENT** |
| **CF-05** | **LOW** | `01` §A, row A9 | Inquiry email-status enum literals in the plan do not match the Approved CHECK constraint. The outbox literals in the same row are correct, confirming this is a wording slip rather than a design position. | `01:19` (A9): "Inquiry email status: **pending/sent/failed**; outbox status: pending/processing/sent/failed". Approved `doc/05:773-774`: `email_status ... CHECK (email_status IN ('email_pending','email_sent','email_failed'))`. Approved `doc/05:788-789` for outbox: `CHECK (status IN ('pending','processing','sent','failed'))` — the plan matches here. Violates the plan's own A25 (`01:35`): "Không phát sinh synonym hoặc enum drift". | An implementer coding from A9 alone writes `'pending'` and the DB CHECK rejects the insert. Fails closed and immediately, so no silent corruption; caught at the first P7 integration test. Contradicts A25 within the same table. | In `01` A9, replace `pending/sent/failed` with `email_pending/email_sent/email_failed`. Leave the outbox literals unchanged. | **TEXT** |
| **CF-06** | **LOW** | `01` §C.2 | The D19 canonical field set v1 uses field names that do not match the Approved inquiry contract, and omits one field that the Approved DTO actually accepts. | Plan `01:73-94` lists `source_page`, `source_path`, `location`, `company_tax_code`, `consent`. Approved DTO `doc/06:196-198` accepts `source_url`, `privacy_consent`; Approved schema `doc/05:768`/`doc/05:772` store `source_url`, `privacy_consent_at`. Neither `location` nor `company_tax_code` exists in the Approved inquiry contract. `source_url` **is** an accepted business input yet appears nowhere in the canonical list. | Direct conflict with the plan's own absolute rule at `01:94` ("**ALL ACCEPTED BUSINESS INPUT FIELDS MUST BE INCLUDED**") and its CI rule ("CI/contract review phải fail nếu DTO nhận thêm business field mà canonical schema version chưa thêm field/version") — the plan's own CI gate would fail at P7 on `source_url`. Fingerprint impact is negligible in practice, since a differing `source_url` under an identical key should replay anyway; the defect is contract self-consistency, not hash correctness. | In `01` §C.2, rename `source_page`/`source_path` to `source_url` and `consent` to `privacy_consent`, matching Approved `06`/`05`. Either remove `location` and `company_tax_code` or retain them explicitly annotated as not accepted by the P0 DTO and therefore always carrying the `missing` marker. Keep the fixed key order and the `v1` version label unchanged. | **TEXT** |

---

## Findings deliberately **not** raised

Recorded so a future reviewer does not re-derive them.

| Candidate concern | Assessment |
|---|---|
| `/media/*` public delivery prefix has no Approved definition | **OBSERVATION, not a finding.** `doc/05:71-73` stores `storage_disk`/`storage_path`/`public_url` without prescribing a public prefix, and `doc/06:179-181` defines only the `/admin/media/:id` API. The plan's `/media/*` is additive with no Approved conflict. Listed inside the CF-01 register for completeness only. |
| `05` module matrix has 40 rows against a 25-module inventory | **Not a discrepancy.** `05:52` states that matrix rows are capabilities, not modules, and that the inventory remains exactly 25. Verified: `03` §4 numbered table = 25. |
| v0.4 SHA-256 equality cannot be reproduced | **Known limitation, already recorded** by the plan at `04:14` and `10:75`. No baseline hash manifest was ever persisted, so only filesystem-timestamp evidence exists (v0.4 files all `2026-07-22`, candidate all `2026-07-25`). Resolves once Git is restored under FV-12. Not a Gate A blocker. |
| Empty `.git` directory | **Already registered** as R-25 (`09:33`, `GATE B BLOCKER`) and as the Pre-P0 prerequisite (`04:12`). Independently confirmed — `git status` fails, `.git` contains only `.` and `..`. Correctly excluded from Gate A. |
| Replay path creates an unauthenticated cheap read path | **Adequately governed** by `01:143`, which permits a separate replay/read abuse limit under four constraints (must not deprive legitimate retry of resolution, must not resend email, must not create records/attempts, must not return data beyond the stable result). The fourth constraint becomes enforceable once CF-03 lands. |
| Approved `README_VERIFY.md` stale `STATIC VALIDATION ONLY` wording | **Already dispositioned** as FV-13 / R-22, DEFERRED WITH GATE, routed to Approved-document governance backlog (`10:71-74`, `09:30`). No Approved file edited. Correct handling. |
